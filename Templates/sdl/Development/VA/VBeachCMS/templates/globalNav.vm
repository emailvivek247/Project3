<style type="text/css">
.active, .inactive {text-align: center; padding-top:0px; padding-bottom:5px; margin:5px}
//.active   {background-color: #3366cc; color: white; font-weight:bold;}
//.inactive {background-color: #efefef; color: blue; }
.active   {color: black; font-weight:bold;}
.inactive {text-decoration: underline;}
</style>
<table border=0 cellpadding=0 cellspacing=0><tr>
#foreach( $dc in $dcs )
  #if($dc.isValid() && $dc.displayOrder >= 0 && $dc.getDefaultTemplateName())
    #set($tf=$templateUtil.getTemplateFilePath($dc.name, $dc.getDefaultTemplateName(), "documents.vm"))
    <td class=#if($indexName == $dc.name )"active"#else"inactive"#end><a class=#if($indexName == $dc.name )"active"#else"inactive"#end href="search.do?indexName=${dc.name}&q=$!{URLEncodedQuery}">${dc.displayName}</a></td>
  #end
#end
</tr></table>