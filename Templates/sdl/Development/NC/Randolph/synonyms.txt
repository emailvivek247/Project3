#put any customized synonyms here
#synonyms with the same meaning should be in one line, separated by an empty space or comma
# if there is comma in the line, the line will be separated by comma
# if there is no comma in the line, the line will be separated by empty spaces
# synonyms are applied after the analyzer. So if your analyzer lowercase letters, you need to put lower cased letters here.

java coffee
eat ate
good, not bad, ok

#only analyzers that treat i.b.m. as one word can really use this synonym pair
i.b.m., ibm
