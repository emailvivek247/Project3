<#-- *Modify columns -->
<table id="searchResult" class="tablesorter">
 <thead><tr>
  <th>${searchResult.datasetConfiguration.getColumn("ACCOUNT_NUM").displayName}</th>
  <th>${searchResult.datasetConfiguration.getColumn("OWNER_NAME1").displayName}</th>
  <th>${searchResult.datasetConfiguration.getColumn("DISPLAY_ADDRESS").displayName}</th>
  <th>${searchResult.datasetConfiguration.getColumn("OWNER_CITY").displayName}</th>
  <th>${searchResult.datasetConfiguration.getColumn("TOT_VAL").displayName}</th>
 </tr></thead>
 <tbody>
<#if searchResult.docs?has_content>
 <#foreach doc in searchResult.docs>
  <#include "document.stl">
 </#foreach>
</#if>
 </tbody>
</table>


<script type="text/javascript">
$(function() {      
    $("#searchResult").tablesorter({widgets: ['zebra']});
}); 
</script>
