<#if (searchResult.docs?has_content && searchResult.docs.size() > 0) >
<#assign ResultId =0>
 <#foreach doc in searchResult.docs>
  <p class="result">
     <br/><b>${searchResult.datasetConfiguration.getColumn("IndexName").displayName}</b>:
${searchResult.highlight(doc, "IndexName")}
 <br/>&nbsp;
 </#foreach>
<#else>
 <#include "noResults.stl"> 
</#if>

