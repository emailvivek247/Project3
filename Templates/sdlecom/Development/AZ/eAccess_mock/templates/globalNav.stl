
<script type="text/javascript">
var timeout         = 500;
var closetimer      = 0;
var ddmenuitem      = 0;
function mopen(id)
{
    mcancelclosetime();
    if(ddmenuitem) ddmenuitem.style.visibility = 'hidden';
    ddmenuitem = document.getElementById(id);
    ddmenuitem.style.visibility = 'visible';
}
function mclose()
{
    if(ddmenuitem) ddmenuitem.style.visibility = 'hidden';
}
function mclosetime()
{
    closetimer = window.setTimeout(mclose, timeout);
}
function mcancelclosetime()
{
    if(closetimer)
    {
        window.clearTimeout(closetimer);
        closetimer = null;
    }
}document.onclick = mclose;
</script>

<#assign indexCount = 0>
<#foreach dc in dcs>
	  <#if dc.isValid() && (dc.displayOrder>=0) && dc.getDefaultTemplateName()?has_content>
	  	<#assign indexCount = indexCount + 1>
	  </#if>	
</#foreach>

<table class="ui-state-default" border=0 cellpadding=0 cellspacing=0 width="100%">
</div>
	<tr>
		<td style="width:100%" >
			<div style="float:left;">
			  <#if indexCount &gt; 0>
			 	<ul class="headerList" float:right;>
				<#foreach dc in dcs>			 
				  <#if dc.isValid() && (dc.displayOrder>=0) && dc.getDefaultTemplateName()?has_content>
				    <li style="padding-top: 4px; padding-bottom: 4px;" class="<#if indexName == dc.name>ui-state-active </#if>ui-state-default">
				      <a href="search.do?indexName=${dc.name}&q=${searchResult.getURLEncodedClearQuery()}">${dc.displayName}</a>
				    </li>
				  </#if>  
				</#foreach>
				</ul>
			   </#if>
			</div>
			</div>
			<div Style="float:right;color:white;">
			<#include "/web-inf/view/admin/layout/loginMenu.stl">
			</div>
			<div style="float:right;" margin-top:17px;color:white;>
				<include><a href="http://apps.supremecourt.az.gov/publicaccess/" style="color:white;  padding-left:100px; " > Help </a>
				
			</div>
		</td>		
	</tr>
</table>
<#if request.getServletContext().getInitParameter("isSecurityEnabled") == "true">
<div style="width:80%;text-align:right;" >
</div>
</#if> 
	



